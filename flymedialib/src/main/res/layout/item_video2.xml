<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="110px"
    android:minHeight="110px"
    android:orientation="vertical">

    <ImageView
        android:layout_margin="4px"
        android:id="@+id/iv_gridview"
        android:layout_width="match_parent"
        android:layout_height="110px"
        android:minHeight="110px"
        android:duplicateParentState="true"
        android:scaleType="fitXY"
        android:src="@drawable/media_default_image" />

    <ImageView
        android:layout_margin="4px"
        android:id="@+id/item_iv01_back"
        android:layout_width="match_parent"
        android:layout_height="110px"
        android:minHeight="110px"
        android:duplicateParentState="true"
        android:scaleType="fitXY"
        android:src="@drawable/rectangle_select" />

    <com.jancar.media.view.MarqueeTextView
        android:id="@+id/tv_gridview"
        android:layout_marginStart="8px"
        android:layout_marginEnd="8px"
        android:layout_marginTop="84px"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#2F000000"
        android:duplicateParentState="true"
        android:ellipsize="marquee"
        android:gravity="center"
        android:marqueeRepeatLimit="1"
        android:singleLine="true"
        android:text="video "
        android:textColor="@color/textcolor"
        android:textSize="18px" />

</RelativeLayout>